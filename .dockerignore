# Keep the Docker build context small and free of local build artifacts.
.git/
dist/
bin/
*.exe
*.log
coverage.*
*.test
*.out

# Local Terraform/provider dev files (not needed to build the provider).
.terraform/
.terraformrc
terraform.rc
*.tfstate
*.tfstate.*
